# Alternative Dockerfile using a different approach
FROM scratch
COPY . /app
WORKDIR /app

# Set up basic environment
ENV PYTHONPATH=/app
ENV FLASK_APP=app.py
ENV PYTHONUNBUFFERED=1

# This is a minimal container that doesn't require external dependencies
# We'll run Python from the host system

EXPOSE 5000
CMD ["echo", "OCR Agent container ready"]